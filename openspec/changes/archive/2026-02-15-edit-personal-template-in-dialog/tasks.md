## 1. 测试先行

- [x] 1.1 在 `EditorView` 相关测试中新增用例：编辑模板保存时构建更新 payload（标题+正文）正确。
- [x] 1.2 在 `EditorView` 相关测试中新增用例：取消编辑时不改变原模板数据引用。

## 2. 前端编辑弹窗实现

- [x] 2.1 在模板面板增加“编辑模板”入口与弹窗状态管理。
- [x] 2.2 实现“从已选模板加载到编辑表单”逻辑，并支持标题与正文编辑。
- [x] 2.3 实现“保存修改”逻辑：调用 `planTemplateStore.updateTemplate` 并刷新模板列表。
- [x] 2.4 实现“取消编辑”逻辑：关闭弹窗且不提交更新。

## 3. 验证与收口

- [x] 3.1 运行前端相关测试（定向 + 全量）并确认通过。
- [x] 3.2 更新任务勾选状态，准备 verify/archive。
