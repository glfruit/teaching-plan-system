## MODIFIED Requirements

### Requirement: Editor page SHALL preserve warm layout and template workflow
The editor page SHALL provide warm-styled editing workflow with two-column desktop layout and mobile-safe actions.

#### Scenario: Shortcut index badge on tabs
- **WHEN** editor tabs are rendered
- **THEN** each tab SHALL expose visible shortcut index aligned to `Alt/Option + number`
- **AND** index order SHALL match label order consistently
