## 1. 主编辑标签效率增强

- [x] 1.1 为主编辑标签增加键盘快捷切换（Alt/Option + 1~4）。
- [x] 1.2 扩展编辑页偏好存储，持久化并恢复当前主标签。
- [x] 1.3 补充主标签语义（`aria-current`）并校验不影响现有定位逻辑。

## 2. 模板编辑弹窗引导增强

- [x] 2.1 计算模板弹窗“下一待完善标签”。
- [x] 2.2 增加快捷跳转按钮并保持 `TipTapEditor` 状态不丢失。

## 3. 测试与验证

- [x] 3.1 更新 `EditorView-layout-blocks` 相关测试（偏好兼容与标签逻辑）。
- [x] 3.2 更新 `EditorView-form-and-editor-style` 相关测试（快捷切换与语义）。
- [x] 3.3 运行定向测试与前端构建并通过。
