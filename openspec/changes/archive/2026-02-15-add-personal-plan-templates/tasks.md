## 1. 后端模板数据与接口

- [x] 1.1 在 `backend` 新增 `PlanTemplate` 数据模型与迁移，字段覆盖整份教案模板所需内容并关联 `teacherId`。
- [x] 1.2 新增模板 REST 路由（`GET/POST/GET:id/PATCH:id/DELETE:id`），并接入 JWT 鉴权。
- [x] 1.3 实现模板归属校验：仅模板拥有者可读写删除，非拥有者请求返回拒绝。
- [x] 1.4 为模板路由补齐后端测试：创建、列表隔离、跨用户越权拒绝、删除成功。

## 2. 前端模板管理与套用流程

- [x] 2.1 在前端新增模板 store/API 封装（列表、创建、更新、删除、详情）。
- [x] 2.2 在编辑页增加“模板面板/弹窗”：支持模板列表展示、关键词检索、保存当前教案为模板。
- [x] 2.3 实现“套用模板（覆盖当前教案）”确认流程：确认后原子替换表单与 `contentJson`，取消不变更。
- [x] 2.4 补齐前端测试：模板套用覆盖行为、取消行为、保存模板调用与成功反馈。

## 3. 保存重开一致性与回归验证

- [x] 3.1 新增/更新测试验证“模板套用 -> 保存 -> 重开”链路下各 section `contentJson` 可恢复。
- [x] 3.2 运行后端相关测试与前端全量测试，确保无回归并记录命令结果。
- [x] 3.3 更新本 change 的任务勾选状态与验收说明，准备进入 verify/archive。
