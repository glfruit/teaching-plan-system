## 1. 测试先行

- [x] 1.1 新增后端测试：模板创建/更新可保存 `tags`，列表支持 `tag` 过滤。
- [x] 1.2 新增前端测试：标签去重与规范化、按标签筛选参数传递。

## 2. 后端实现

- [x] 2.1 为 `PlanTemplate` 增加 `tags` 字段与迁移。
- [x] 2.2 更新 `/plan-templates` 路由的创建/更新 schema，接收 `tags`。
- [x] 2.3 更新模板列表查询逻辑，支持 `?tag=<value>` 过滤。

## 3. 前端实现

- [x] 3.1 更新 `planTemplate` store 类型与 API 调用，支持 `tags` 与 `tag` 过滤参数。
- [x] 3.2 在模板面板与编辑弹窗增加标签输入与预设标签快捷添加。
- [x] 3.3 增加“按标签筛选”快速入口，并与关键词查询联动。

## 4. 验证与收口

- [x] 4.1 运行后端相关测试与前端全量测试并通过。
- [x] 4.2 更新任务勾选状态，准备 verify/archive。
