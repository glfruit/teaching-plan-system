## 1. 标签组件抽离

- [x] 1.1 新增 `EditorLayoutTabs.vue`，承载编辑页吸顶标签分区 UI。
- [x] 1.2 新增 `TemplateEditTabs.vue`，承载模板编辑弹窗标签导航 UI。
- [x] 1.3 在 `EditorView.vue` 中替换为组件调用，保持原有交互行为。

## 2. 标签交互强化

- [x] 2.1 保持 section↔tab 联动函数可复用并可测试。
- [x] 2.2 主编辑标签与模板弹窗标签支持移动端横向滚动，避免小屏拥挤。

## 3. 测试与验证

- [x] 3.1 更新/新增编辑页测试，覆盖组件化后的标签结构与联动逻辑。
- [x] 3.2 运行编辑页相关测试并通过。
- [x] 3.3 运行前端构建并通过。
