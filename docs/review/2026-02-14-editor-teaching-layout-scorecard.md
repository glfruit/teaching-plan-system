# 教案编辑增强验收评分卡

> 适用范围：`codex/editor-teaching-layout` 并行开发分支回收验收
> 评审人：Codex（AI）+ 业务确认（你）
> 结论规则：存在任一 `阻塞` 则不可合并

---

## 评分说明

- `通过`：需求已满足，且有代码/测试证据
- `风险`：主路径可用，但存在可预见问题或证据不足
- `阻塞`：未满足关键要求或存在数据正确性问题

---

## A. 功能完整性

1. 工具栏插入入口
- 检查点：可插入 `时间轴`、`步骤卡`、`三栏块`
- 结果：`待评审`
- 证据：
- 备注：

2. Slash 插入入口
- 检查点：可通过 `/时间轴`、`/步骤卡`、`/三栏块` 插入
- 结果：`待评审`
- 证据：
- 备注：

3. 节点内操作能力
- 检查点：编辑、删除、复制、移动
- 结果：`待评审`
- 证据：
- 备注：

4. 插入容错
- 检查点：非法位置插入有提示并自动回退到可插入位置
- 结果：`待评审`
- 证据：
- 备注：

## B. 数据正确性（阻塞级）

5. 保存后可恢复
- 检查点：保存后重开，三类块结构与内容一致
- 结果：`待评审`
- 证据：
- 备注：

6. 结构化持久化
- 检查点：使用 ProseMirror JSON 语义，不是纯 HTML 字符串拼装
- 结果：`待评审`
- 证据：
- 备注：

7. 未识别节点降级
- 检查点：未知节点不崩溃、不丢内容，可降级显示
- 结果：`待评审`
- 证据：
- 备注：

## C. 编辑体验

8. 占位文案
- 检查点：首次插入具有可替换示例文本
- 结果：`待评审`
- 证据：
- 备注：

9. 新手路径效率
- 检查点：按钮插入不超过 2 次点击完成
- 结果：`待评审`
- 证据：
- 备注：

10. 键盘路径可用
- 检查点：Slash + 上下选择 + 回车可完成插入
- 结果：`待评审`
- 证据：
- 备注：

## D. 回归兼容

11. 旧教案兼容
- 检查点：旧数据打开、编辑、保存正常
- 结果：`待评审`
- 证据：
- 备注：

12. 现有编辑功能回归
- 检查点：图片、表格、基础格式能力无退化
- 结果：`待评审`
- 证据：
- 备注：

13. 导出链路可用
- 检查点：导出结果可读，结构不乱
- 结果：`待评审`
- 证据：
- 备注：

## E. 测试门禁

14. 节点/命令单元测试
- 检查点：新增节点与命令有自动化测试
- 结果：`待评审`
- 证据：
- 备注：

15. 组件测试
- 检查点：工具栏与 Slash 入口有测试覆盖
- 结果：`待评审`
- 证据：
- 备注：

16. 集成测试
- 检查点：保存-重开-继续编辑流程有测试
- 结果：`待评审`
- 证据：
- 备注：

17. 全量测试通过
- 检查点：`frontend` 与 `backend` 测试通过
- 结果：`待评审`
- 证据：
- 备注：

## F. 性能稳定性

18. 插入响应
- 检查点：常规机器主观操作无明显卡顿
- 结果：`待评审`
- 证据：
- 备注：

19. 高频编辑稳定
- 检查点：连续插入/删除 30 次不崩溃
- 结果：`待评审`
- 证据：
- 备注：

20. 长文档可编辑
- 检查点：20+ 排版块仍可编辑、保存
- 结果：`待评审`
- 证据：
- 备注：

---

## 最终结论

- 总体结论：`待评审`
- 合并建议：`待评审`
- 阻塞项列表：
- 风险项列表：
- 建议修复顺序：

---

## 执行命令（评审时）

```bash
cd /Users/glfruit/Workspace/Projects/teaching-plan-system/.worktrees/codex/editor-teaching-layout/frontend
bun test
```

```bash
cd /Users/glfruit/Workspace/Projects/teaching-plan-system/.worktrees/codex/editor-teaching-layout/backend
bun test
```
